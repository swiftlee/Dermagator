{
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "client": "yarn workspace client start",
    "server": "yarn workspace server start",
    "start": "--kill-others-on-fail && yarn client", 
    "heroku-postbuild": "yarn && yarn workspace server build && yarn workspace client build"
  },
  "dependencies": {
    "concurrently": "^5.0.0",
    "yarn": "^1.19.1"
  },
  "comments": {
	"start": "concurrently --kill-others-on-fail \"yarn client\" \"yarn server\"",
  }
}
